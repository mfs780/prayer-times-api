# Comma-separated list of origins allowed via CORS. Defaults to "*" if unset.
# In dev with `*`, every origin (incl. your local sites on other ports) can call
# the service. For production, lock it down explicitly:
# ALLOWED_ORIGINS=https://example.com,https://another.example.com
